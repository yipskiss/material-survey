# 재질 시각 평가 — GitHub Pages + Google Sheets 설정 방법

구조: **GitHub Pages**(설문 사이트 호스팅) + **Google Sheets**(응답 데이터 저장).
사이트는 로그인 없이 아무 기기(아이패드 포함)에서 열 수 있고, 응답은 재료 하나 끝날 때마다
구글 시트에 한 줄씩 실시간으로 쌓입니다.

---

## 1단계 — 구글 시트 + Apps Script 만들기 (약 5분)

1. [sheets.new](https://sheets.new) 접속 → 새 스프레드시트 생성 (이름 예: `재질평가 응답`)
2. 메뉴 **확장 프로그램 → Apps Script** 클릭
3. 편집기에 기본으로 있는 코드를 지우고, 이 폴더의 **apps-script.gs** 내용 전체를 붙여넣기 → 저장(💾)
4. 오른쪽 위 **배포 → 새 배포** 클릭
   - 유형 선택(⚙️): **웹 앱**
   - 다음 사용자 인증 정보로 실행: **나**
   - 액세스 권한이 있는 사용자: **모든 사용자** ← 중요!
   - **배포** 클릭 → 권한 승인 (내 계정 선택 → "고급" → "이동" → 허용)
5. 나오는 **웹 앱 URL**(`https://script.google.com/macros/s/…/exec`)을 복사

## 2단계 — index.html에 URL 넣기

`index.html`을 메모장으로 열어 상단 설정 부분에 붙여넣기:

```
var ENDPOINT='여기에 /exec URL';
var SHEET_URL='여기에 스프레드시트 주소 (선택, 관리 화면에 바로가기 버튼 생김)';
```

> Claude에게 URL을 주면 대신 넣고 커밋해 드립니다.

## 3단계 — GitHub에 올리기

1. [github.com/new](https://github.com/new) 에서 새 저장소 생성 (예: `material-survey`, **Public**)
2. 이 폴더(`github-pages`)에서 push:

```
git remote add origin https://github.com/<아이디>/material-survey.git
git push -u origin main
```

(푸시할 때 브라우저 로그인 창이 뜨면 본인 GitHub 계정으로 승인)

3. 저장소 → **Settings → Pages** → Source: **Deploy from a branch**, Branch: **main / (root)** → Save
4. 1~2분 후 `https://<아이디>.github.io/material-survey/` 에서 사이트 접속 가능

## 데이터 확인

- 응답은 구글 시트의 `responses` 탭에 한 줄씩 쌓입니다 (피험자, 재료, 문항 9개, 시각)
- 분석용 파일: 시트에서 **파일 → 다운로드 → Microsoft Excel(.xlsx)**
- 같은 피험자의 같은 재료가 중복 전송되면 최초 응답만 저장됩니다

## 인터넷이 끊기면?

응답은 항상 기기에 먼저 저장되고, 연결이 돌아오면 자동으로 전송됩니다
(상단에 "전송 대기 n건" 배너가 떴다가 사라짐). 시트 도착 여부는
관리 화면의 "시트 연결됨" 표시와 대기분 전송 버튼으로 확인할 수 있어요.

## 주의

- Apps Script를 **수정한 뒤에는** 배포 → 배포 관리 → ✏️ → 버전: 새 버전 → 배포를 해야 반영됩니다
- API_KEY(`hxlab-material-2026`)는 apps-script.gs와 index.html 양쪽에 같은 값이어야 합니다
